# 系统架构设计

## 需求
{{REQUIREMENTS}}

## 你的任务
设计系统架构，输出以下格式的文档到 `{{ARCHITECTURE_FILE}}`：

### 模块划分
| 模块 | 负责内容 | 涉及文件 | 依赖关系 |
|---|---|---|---|
| ... | ... | ... | ... |

### 执行顺序
- 可并行的模块组
- 需要串行的依赖链

### 接口约定
- 模块间的约定（API 路径、数据格式等）

### 数据流设计（如适用）
- 数据如何在模块间流转

### 错误处理策略（如适用）
- 全局错误处理方案

### 测试策略（如适用）
- 各模块的测试方法

## 重要约束
- ⚠️ **必须使用指定的分支名** `{{BRANCH}}`，不要自己命名分支
- **开始工作前**，创建并切换到独立分支：`git checkout -b {{BRANCH}}`
- **完成后**，提交修改：`git add -A; git commit -m "{{BRANCH}}: 架构设计"`
- **完成后创建信号文件** `{{SIGNAL_FILE}}`，JSON 格式如下：

```json
{
  "status": "done",
  "brain_id": "<从 artifact 路径中提取你的 conversation-id>",
  "branch": "{{BRANCH}}",
  "files_changed": ["列出你修改的所有文件的相对路径"],
  "summary": "一句话描述架构设计的核心方案",
  "skills_used": ["列出你读取和使用的 skill 名称"],
  "errors": []
}
```

## 项目上下文
详见 `{{CONTEXT_FILE}}`，请先阅读该文件获取项目背景。

> ⚠️ **如果任务失败**，仍然必须创建信号文件，但将 `status` 改为 `"failed"`，并在 `errors` 中描述原因。
